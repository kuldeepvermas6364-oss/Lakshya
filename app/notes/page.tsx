"use client";
import { useState } from "react";
export default function NotesPage(){const [notes,setNotes]=useState(['Electrostatics — formula sheet','Solutions — quick revision','Calculus — important methods']); return <main className="page"><div className="hero-row"><div><p className="eyebrow">YOUR KNOWLEDGE BASE</p><h1>Notes</h1><p className="muted">Keep your revision material organized and easy to find.</p></div><button className="primary" onClick={()=>setNotes([...notes,'New study note'])}>+ New note</button></div><div className="community-search"><span>⌕</span><input placeholder="Search your notes..."/></div><div className="subject-grid" style={{marginTop:18}}>{notes.map((n,i)=><article className="subject" key={i}><div className="subject-top"><div className="subject-icon">PDF</div><span>Note</span></div><h3>{n}</h3><small>Last updated today · Private</small><div style={{marginTop:14}}><button className="secondary">Open note →</button></div></article>)}</div></main>}
