"use client";
import { useEffect, useState } from "react";
export default function FocusPage(){const [seconds,setSeconds]=useState(25*60);const [running,setRunning]=useState(false);useEffect(()=>{if(!running)return;const id=setInterval(()=>setSeconds(s=>s>0?s-1:25*60),1000);return()=>clearInterval(id)},[running]);const m=String(Math.floor(seconds/60)).padStart(2,'0'),s=String(seconds%60).padStart(2,'0');return <main className="page"><div className="hero-row"><div><p className="eyebrow">DEEP WORK</p><h1>Focus Mode</h1><p className="muted">Choose one task. Remove distractions. Make progress.</p></div></div><section className="panel focus-panel" style={{maxWidth:700,margin:'0 auto',minHeight:520}}><div className="panel-head"><div><p className="eyebrow">CURRENT TASK</p><h2>Physics — Electrostatics</h2></div><span className="live-dot">● {running?'FOCUSING':'READY'}</span></div><div className="timer"><div className="timer-circle"><span>{m}:{s}</span><small>FOCUS</small></div></div><div className="timer-controls"><button className="primary" onClick={()=>setRunning(!running)}>{running?'Pause':'Start focus'}</button><button className="secondary" onClick={()=>{setRunning(false);setSeconds(25*60)}}>Reset</button></div><p className="muted center">25 minutes focus · 5 minutes break · repeat</p></section></main>}
