"use client";
import { useState } from "react";
const initial=[['08:30','Physics','Electrostatics','90 min'],['10:15','Practice','JEE Mixed Questions','60 min'],['13:00','Chemistry','Solutions revision','45 min'],['21:00','Mathematics','Integrals','90 min']];
export default function PlannerPage(){const [items,setItems]=useState(initial); const [done,setDone]=useState<number[]>([]); return <main className="page"><div className="hero-row"><div><p className="eyebrow">PLAN WITH PURPOSE</p><h1>Study Planner</h1><p className="muted">Build a realistic day, then focus on one session at a time.</p></div><button className="primary" onClick={()=>setItems([...items,['22:30','Revision','Quick recap','30 min']])}>+ Add session</button></div><section className="panel"><div className="panel-head"><div><p className="eyebrow">TODAY</p><h2>Tuesday schedule</h2></div><span className="tag">{done.length}/{items.length} COMPLETE</span></div>{items.map((x,i)=><div className="timeline-item" key={i}><div className="time">{x[0]}</div><div className="dot"/><div className="session"><span className="tag">{x[1]}</span><h3>{x[2]}</h3><small>{x[3]}</small></div><button className={done.includes(i)?'primary':'secondary'} onClick={()=>setDone(done.includes(i)?done.filter(v=>v!==i):[...done,i])}>{done.includes(i)?'Done':'Start'}</button></div>)}</section></main>}
