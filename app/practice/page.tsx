"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { onAuthStateChanged } from "firebase/auth";
import { auth } from "@/lib/firebase";
import { savePracticeAttempt } from "@/lib/study-storage";

const curriculum = {
  Physics: ["Electric Charges & Fields", "Electrostatic Potential & Capacitance", "Current Electricity", "Moving Charges & Magnetism", "Magnetism & Matter", "Electromagnetic Induction", "Alternating Current", "Electromagnetic Waves", "Ray Optics & Optical Instruments", "Wave Optics", "Dual Nature of Radiation & Matter", "Atoms", "Nuclei", "Semiconductor Electronics"],
  Chemistry: ["Solutions", "Electrochemistry", "Chemical Kinetics", "d- and f-Block Elements", "Coordination Compounds", "Haloalkanes & Haloarenes", "Alcohols, Phenols & Ethers", "Aldehydes, Ketones & Carboxylic Acids", "Amines", "Biomolecules", "Polymers", "Chemistry in Everyday Life"],
  Mathematics: ["Relations & Functions", "Inverse Trigonometric Functions", "Matrices", "Determinants", "Continuity & Differentiability", "Applications of Derivatives", "Integrals", "Applications of Integrals", "Differential Equations", "Vector Algebra", "Three Dimensional Geometry", "Probability"]
} as const;
type Subject = keyof typeof curriculum;
type Question = { question:string; options:string[]; answer:number; explanation:string };

export default function PracticePage(){
  const [subject,setSubject]=useState<Subject>("Physics");
  const [chapter,setChapter]=useState<string>(curriculum.Physics[0]);
  const [questions,setQuestions]=useState<Question[]>([]);
  const [q,setQ]=useState(0); const [selected,setSelected]=useState<number|null>(null);
  const [uid,setUid]=useState<string|null>(null); const [saved,setSaved]=useState(false);
  const [loading,setLoading]=useState(false); const [error,setError]=useState("");
  useEffect(()=>onAuthStateChanged(auth,u=>setUid(u?.uid??null)),[]);
  function changeSubject(value:Subject){setSubject(value);setChapter(curriculum[value][0]);setQuestions([]);setSelected(null);setQ(0);}
  async function generate(){setLoading(true);setError("");setQuestions([]);setSelected(null);setQ(0);try{const r=await fetch("/api/ai/quiz",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({subject,chapter})});const data=await r.json();if(!r.ok)throw new Error(data.error||"Quiz generation failed");setQuestions(data.questions||[]);}catch(e){setError(e instanceof Error?e.message:"Could not generate quiz.")}finally{setLoading(false)}}
  async function answer(i:number){setSelected(i);if(uid&&!saved&&questions[q]){try{await savePracticeAttempt(uid,{subject,chapter,correct:i===questions[q].answer});setSaved(true)}catch(e){setError(e instanceof Error?e.message:"Could not save attempt")}}}
  function next(){setQ(x=>x+1);setSelected(null);setSaved(false)}
  const current=questions[q];
  return <main className="page practice-page"><div className="hero-row"><div><p className="eyebrow">PRACTICE ARENA</p><h1>Practice from your real syllabus.</h1><p className="muted">Questions are generated for the chapter you choose and your attempts are saved to Firebase when signed in.</p></div><Link className="primary" href="/study">Study hub →</Link></div>
    <section className="practice-builder panel"><div><p className="eyebrow">CHOOSE CHAPTER</p><h2>Generate a fresh quiz</h2></div><div className="builder-grid"><label>Subject<select value={subject} onChange={e=>changeSubject(e.target.value as Subject)}>{(Object.keys(curriculum) as Subject[]).map(s=><option key={s}>{s}</option>)}</select></label><label>Chapter<select value={chapter} onChange={e=>{setChapter(e.target.value);setQuestions([]);setSelected(null);setQ(0)}}>{curriculum[subject].map(c=><option key={c}>{c}</option>)}</select></label><button className="primary" onClick={generate} disabled={loading}>{loading?"Generating…":"Generate quiz ✦"}</button></div></section>
    {error&&<div className="practice-error">{error}</div>}
    {!current&&!loading&&<section className="practice-empty panel"><div>✦</div><h2>No sample questions here.</h2><p className="muted">Choose a chapter and generate a fresh syllabus-aligned quiz with Lakshya AI.</p></section>}
    {loading&&<section className="practice-empty panel"><div className="loader">✦</div><h2>Building your quiz…</h2><p className="muted">Lakshya AI is preparing chapter-specific questions.</p></section>}
    {current&&<section className="practice-card panel"><div className="practice-meta"><span className="tag active-tag">{subject}</span><span className="tag">{chapter}</span><span className="muted">QUESTION {q+1} / {questions.length}</span></div><h2>{current.question}</h2><div className="answer-grid">{current.options.map((a,i)=><button key={a} onClick={()=>answer(i)} className={`answer-option ${selected===i?(i===current.answer?'correct':'wrong'):''}`}><span>{String.fromCharCode(65+i)}</span><span>{a}</span>{selected===i&&<b>{i===current.answer?'✓':'×'}</b>}</button>)}</div>{selected!==null&&<div className={`answer-feedback ${selected===current.answer?'good':'retry'}`}>{selected===current.answer?`Correct! ${current.explanation}`:`Not quite. ${current.explanation}`}</div>}<div className="practice-footer"><span className="muted">{uid?'✓ Firebase attempt saved':'Sign in to save your attempt'}</span>{q<questions.length-1?<button className="primary" onClick={next} disabled={selected===null}>Next question →</button>:<button className="primary" onClick={generate}>Generate another →</button>}</div></section>}
    <style jsx>{`.practice-builder{margin:0 auto 16px;padding:20px;max-width:1000px}.practice-builder h2{margin:5px 0 0;font-size:20px}.builder-grid{display:grid;grid-template-columns:1fr 1.5fr auto;gap:10px;margin-top:15px;align-items:end}.builder-grid label{font-size:9px;color:#777f90;font-weight:800;text-transform:uppercase;letter-spacing:.5px}.builder-grid select{display:block;width:100%;margin-top:6px;border:1px solid #e1e4ec;background:#fff;border-radius:11px;padding:11px;font-size:11px;color:#252a3b;outline:none}.practice-empty{text-align:center;padding:50px 20px;max-width:900px;margin:auto}.practice-empty>div{font-size:34px;color:#635bff}.practice-empty h2{margin:10px 0 5px}.loader{animation:spin 1s linear infinite}.practice-error{max-width:900px;margin:0 auto 14px;padding:11px 14px;border-radius:11px;background:#fff2f4;color:#a52f48;font-size:10px}.practice-card{max-width:900px;margin:0 auto;padding:26px;animation:riseIn .35s ease both}.practice-meta{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.practice-meta .muted{margin-left:auto;font-size:9px}.practice-card>h2{font-size:23px;line-height:1.45;margin:24px 0}.answer-grid{display:grid;gap:10px}.answer-option{display:grid;grid-template-columns:34px 1fr 20px;align-items:center;gap:10px;width:100%;text-align:left;border:1px solid #e4e6ee;background:#fff;border-radius:13px;padding:14px;color:#34394a;transition:.2s;cursor:pointer}.answer-option:hover{transform:translateX(3px);border-color:#c9c5ff;box-shadow:0 8px 20px rgba(38,44,90,.07)}.answer-option>span:first-child{width:28px;height:28px;border-radius:8px;display:grid;place-items:center;background:#f1f2f8;color:#635bff;font-weight:800}.answer-option.correct{border-color:#8ce0c2;background:#effcf7}.answer-option.wrong{border-color:#f2b1bd;background:#fff4f6}.answer-feedback{margin-top:14px;padding:12px;border-radius:11px;font-size:11px;line-height:1.6;font-weight:700}.answer-feedback.good{background:#ecfbf5;color:#087b55}.answer-feedback.retry{background:#fff1f3;color:#b12f4a}.practice-footer{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-top:22px;padding-top:18px;border-top:1px solid #eceef3}@keyframes spin{to{transform:rotate(360deg)}}@media(max-width:700px){.builder-grid{grid-template-columns:1fr}.practice-card{padding:18px}.practice-meta .muted{width:100%;margin-left:0}.practice-card>h2{font-size:18px}.practice-footer{align-items:flex-start;flex-direction:column}}`}</style>
  </main>
}
