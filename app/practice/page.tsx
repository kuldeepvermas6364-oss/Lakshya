"use client";
import { useState } from "react";
const qs=[['Physics','An electric field is conservative when its work is independent of the path. Which statement follows?',['Potential difference is path independent','Field must be uniform','Charge must be zero','Potential must be zero']],['Chemistry','For an ideal solution, which law describes the partial vapour pressure of a component?',['Raoult’s law','Henry’s law','Boyle’s law','Graham’s law']]];
export default function PracticePage(){const [q,setQ]=useState(0);const [selected,setSelected]=useState<number|null>(null);return <main className="page"><div className="hero-row"><div><p className="eyebrow">PRACTICE ARENA</p><h1>Practice</h1><p className="muted">Chapter-wise questions with instant feedback. JEE-ready, one question at a time.</p></div><span className="tag">QUESTION {q+1} / {qs.length}</span></div><section className="panel"><span className="tag">{qs[q][0]}</span><h2 style={{fontSize:22,margin:'14px 0 24px'}}>{qs[q][1]}</h2>{(qs[q][2] as string[]).map((a,i)=><button key={a} onClick={()=>setSelected(i)} className="secondary" style={{display:'block',width:'100%',textAlign:'left',marginBottom:10,padding:15}}>{String.fromCharCode(65+i)}. {a}{selected===i && i===0?' ✓':''}</button>)}<div style={{display:'flex',justifyContent:'space-between',marginTop:20}}><span className="muted">Difficulty: JEE Main</span><button className="primary" onClick={()=>{setQ((q+1)%qs.length);setSelected(null)}}>Next question →</button></div></section></main>}
